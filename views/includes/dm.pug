mixin DirectMessage(userClass, image, text, time)
  li(class=`${userClass}`)
    .app--avatar(style=`background-image: url(${image})`)
      img(src=image)
    p.app--message--text #{text}
    p.app--message--timestamp #{time}
