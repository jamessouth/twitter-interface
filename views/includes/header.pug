header.circle--header(style=`background-image: url(${user[2]}/1500x500)`)
  .bounds
    .circle--fluid
      .circle--fluid--cell
        a.logo(href='/')
          include logoSVG.pug
          h1 #{user[0] ? '@' + user[0] : ''}
      .circle--fluid--cell.align-right
        nav
          ul.circle--inline
            li: a Sign Out
