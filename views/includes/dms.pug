include dm.pug
.grid-33
  .app--section
    ul.app--message--list
      li
        h3 Conversation with
          a  #{dmConvoWith}
        ul.app--message--conversation
          each val in dms
            +DirectMessage(val.sender.classToApply, val.sender.img, val.text, val.created)
